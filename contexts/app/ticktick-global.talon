^quick task [<phrase>] [over]:
    key("shift-alt-cmd-ctrl-a")
    sleep(200ms)
    insert(phrase)

^toggle tick mini: key("shift-alt-cmd-ctrl-o")
# Rebound, didn't trust it
^toggle tick main: key("shift-alt-cmd-ctrl-e")
# Rebound, didn't trust it
^toggle tick pomo: key("shift-alt-cmd-ctrl-p")